---
description: Capture portable session state into .claude/handoff/state.md for resuming in a new session
---

Activate the `handoff` skill and execute its workflow exactly as documented, including the verification phase. The skill captures the current session into a self-contained briefing that a fresh session can use to resume work without losing context.
